[project]
name = "comfyui-llm-prompt-studio"
version = "1.0.6"
description = "SDXL prompt generation and critique via local LLMs (LM Studio / any local OpenAI-compatible server)"
readme = "README.md"
license = { text = "MIT" }
dependencies = ["requests", "numpy", "pillow>=9.1.0"]
authors = [{ name = "Ivas1981" }]
urls = { Repository = "https://github.com/Ivas1981/comfyui_llm_prompt_studio" }

[tool.comfy]
DisplayName = "ComfyUI LLM Prompt Studio"
